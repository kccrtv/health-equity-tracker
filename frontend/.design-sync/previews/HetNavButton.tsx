import HetNavButton from '../../src/styles/HetComponents/HetNavButton'

export const Collapsed = () => (
  <HetNavButton label="Explore the Data" onClick={() => {}} />
)

export const Expanded = () => (
  <HetNavButton label="Our Initiatives" onClick={() => {}} isExpanded />
)
